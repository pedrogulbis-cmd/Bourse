tradingview-screener>=3.2.0
requests>=2.31
tqdm>=4.65
cryptography>=42.0.0
# tvdatafeed s'installe différemment (pas un simple "pip install") :
# pip install --upgrade --no-cache-dir git+https://github.com/rongardF/tvdatafeed.git
